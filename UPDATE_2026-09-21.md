# 2026-09-21 更新说明：GW1N(R)-9C 内置 Flash 烧录修复

本次更新针对 CH32 下载器可以下载 SRAM、但 GW1N(R)-9C 内置 Flash 擦写后验证失败的问题。固件按 BL702 对齐收包、缓冲和 JTAG 执行行为，并将匹配的 Flash DR32 操作改为完整事务连续输出。已验证版本通过单页实际读回、openFPGALoader 完整 Flash 烧录和独立重新加载；用户确认 FPGA 完全断电重上电后 LED 仍闪烁。

## 对比基线

- 远程仓库：`https://github.com/dxxxxdx/CH32_FT232`。
- 2026-09-21 执行 fetch 后，`origin/master` 为 `668b64e4fc595e6d8f39ce6e5986dad297bccd70`，提交日期为 2026-09-20。
- 当前分支为 `1n9fix`，`HEAD` 与 `origin/master` 相同，双方独有提交数均为 0。本次更新仍在未提交工作区中，因此以 `git diff origin/master` 加新增未跟踪文件为准。
- 检查时，已跟踪文件的差异为 28 个文件、文本新增 919 行、删除 1186 行，其中包含根目录 ELF 的二进制变化；该统计不包含新增源码、工具、日志及本文。
- BL702 行为参考为本地 `RV-Debugger-BL702` 的 `1b27d3f74dfc7cfb157e6560ca8a3e7d964b391a`，主要对照实际编译的 `firmware/app/usb2uartjtag/jtag_process.c`，以及 USB 发送和控制请求实现。

## 主要差异

| 项目 | 远程 master | 当前工作区 |
| --- | --- | --- |
| 编译板型 | 固定 20PINOUT | 固定 22PINOUT，覆盖旧 CMake 缓存 |
| JTAG RX 缓冲 | 4096 B | 4096 B |
| JTAG TX 缓冲 | 512 B | 1024 B，与参考 BL702 对齐 |
| 收包调度 | 合并接收，保留 2 ms 间隔超时 | 普通包立即执行，匹配编程页头后按 BL702 收集批次 |
| 批次执行 | 有时钟预算，可因 TX 背压让出 | 全批次执行期间屏蔽可屏蔽中断，不拆批回包 |
| Flash DR32 | 已有 `0x71` MSB 整字合并和延迟退出 | 增加 `0x75` 首个擦除地址、LSB 形式及进入序列暂存，连续输出完整事务 |
| 擦除等待 | 按 HCLK 时基连续输出约 180 ms | MCU 本地连续输出 600000 拍，参考 BL702 为 150000 拍 |
| USB 空状态 IN | 依赖原有延迟及 bit mode 门控，新 OUT 可撤销未发送状态包 | 按最后真实 IN 计时，超过 1 ms 后允许空闲状态回包，保留已提交 IN 的生命周期 |
| ACM 调试 | 无专用 JTAG 追踪模块 | 新增可选追踪，默认关闭；与 UART 转发互斥 |

**远程 master 的 RX 已经是 4 KiB。** 本次相对 master 的缓冲容量变化是 TX 从 512 B 增至 1 KiB；其余重点是收包边界、执行连续性和 USB 交接，不能将本次修复概括成“把 RX 扩到 8 KiB”。

## Flash 事务与时序修正

主要实现位于 [jtagGowinFlash.c](jtag_core/jtagGowinFlash.c)、[jtagManager.c](jtag_core/jtagManager.c) 和 [GPIO_Cfg.c](gpio_toggle/GPIO_Cfg.c)。

对于已识别的 `0x71` 编程及 `0x75` 首个擦除地址，先收齐进入 Shift-DR 的三拍、24+7+1 位数据以及返回 Idle 的后缀。匹配的进入序列暂存期间，物理 TAP 保持在 Idle；数据完整后，MCU 连续执行：

```text
Idle → 三拍进入 Shift-DR → 连续 DR32 → 两拍 Update/Idle → 本地等待
```

该路径中间不再取 USB 数据或解析下一条 MPSSE 命令。支持 MSB `0x11/0x13`、LSB `0x19/0x1B` 数据形式，以及 openFPGALoader 的两拍和 Gowin 的七拍返回形式；七拍返回中的额外 Idle 并入本地等待窗口。

仅复用一份 4 字节整字缓存，并保存进入序列和末拍。控制用 TAP/DR32 状态由 Gowin 内核负责，ACM 观察器不参与引脚控制。开始暂存后遇到不支持的后缀，会进入明确的 `SEQUENCE_FAULT`，等待主机复位及重新同步 TAP。

当前等待参数如下：

| 位置 | MCU 本地连续时钟 |
| --- | ---: |
| 观察到 IR `0x09` 后，后续匹配的 Idle→Shift-IR 入口 | 1200 拍 |
| `0x75` 擦除地址提交后 | 600000 拍 |
| `0x71` 首个 DR32 地址提交后 | 32 拍 |
| `0x71` 后续数据字提交后 | 24 拍 |

匹配的后续主机 Idle 请求由本地等待覆盖，避免重复输出。整字输出及连续时钟保留 BL702 的 `volatile` 两次循环延时结构；普通 TMS 路径也对齐其边沿顺序。位模式长度用 `uint16_t` 承载，保留参考实现的 1～256 拍行为。

同一 GPIO 循环的软件计时参考约为：600000 拍 337.5 ms、1200 拍 675 μs、32 拍 18 μs、24 拍 13.5 μs。这些不是示波器或逻辑分析仪实测值；跨 MCU、编译选项和代码布局不能直接复用真实时间结论。MPSSE `0x86` 分频命令仍只消费参数，主机显示的频率不等于实际 GPIO TCK。

前一版 ATOMIC_COMMIT 使用相同缓冲、等待拍数和 GPIO 循环，单页实验仍失败；DR_TRANSACTION 进一步暂存三拍进入序列后通过。因此现有证据支持“完整 DR 事务连续执行”是起效的关键。两版擦除前缀都装在单个 USB 包内，现有证据不足以归因于 USB 丢包，也未测出 FPGA 可容忍的最大停顿时间。

## 缓冲和 USB 交接

收包策略集中在 [ftdiJtagService.c](jtag_core/ftdiJtagService.c)，USB 端点交接位于 [ft232Usbd.c](usb_bsp/ft232Usbd.c)。

- 普通 OUT 一包即一批；仅扫描新批次首包的前 32 字节，匹配 `4B 03 03 1B 06 71` 后进入编程页收集。
- 编程页中的 64 B 包继续收集，非零短包结束批次。达到 4096 B 后，等下一次 OUT 触发执行；触发包留给下一批，避免覆盖或丢弃。
- 区分“没有 USB 包”和真实零长度 OUT。普通 ZLP 不结束编程页；缓冲已满时，下一次 ZLP 也可以触发满批次执行。
- 移除原来的 2 ms 收包超时和执行时钟预算。批次执行完后清 RX 索引，半条 MPSSE 命令与整字暂存可以跨批保留。
- 收集、执行时不提交新的 IN。真实回包由 `31 60` 加最多 62 B 数据组成；TX 跨环尾时借用两段数据拼入现有 USB 包，避免人为制造短包，也不增加一份 62 B 中间缓存。
- 空闲状态 IN 采用 BL702 的计时行为：距最后真实数据提交超过 1 ms 后允许发送 `31 60`；状态包本身不刷新计时。已排队的 IN 不因新 OUT 到达而撤回。
- 通道 A 的 SIO reset、purge RX、purge TX 都清整个 MPSSE 内核及队列。USB reset/configuration 交接时先 NAK OUT，主循环完成复位后再开放接收，避免复位后的首包被当旧数据清掉。
- 通道 A 的 SET_BITMODE 接受设置，不再隐式清解析器或回包。TX 容量不足则报告 `TX_OVERFLOW` 并停止执行，等待 reset；这一显式错误处理与 BL702 忽略写环形队列返回值的做法不同。

## 22PINOUT、ACM 与构建

[CMakeLists.txt](CMakeLists.txt) 固定使用新 [22pinout.h](boardtype/22pinout.h)，由 [BoardConfig.h](boardtype/BoardConfig.h) 统一提供 JTAG、UART 和 LED 配置。

| 信号 | 引脚 |
| --- | --- |
| TCK | PA6 |
| TMS | PA7 |
| TDI | PA5 |
| TDO | PA4 |
| 可选 UART TX/RX | PB6/PB7 |

22PINOUT 没有运行灯，CMake 强制关闭 `RUN_LED_ENABLE`。UART 配置接受 `PB67` 或 `DISABLED`，拒绝沿用 PA2/PA3。递归收集源码时排除日志快照、固件归档、主机工具和独立直驱诊断目录，避免第二个入口混入正常固件。

`JTAG_ACM_TRACE` 默认 `OFF`，关闭时追踪调用及参数求值被编译期宏移除。启用时，CDC 专用于延迟输出 JTAG 事件、计数和周期状态；它会增加观察开销，因此开启追踪后的时序需要单独验证。

**关闭 ACM 调试与关闭整个 CDC 接口是两个配置。** CMake 的 UART 转发默认仍为 `PB67`；要匹配已验证的纯 FT2232/JTAG 构建，需要同时设置 `JTAG_ACM_TRACE=OFF`、`UART_FORWARD_PORT_SELECT=DISABLED`。

对应构建命令如下，仅用于复现配置；本文整理时没有重新编译：

```sh
cmake -S . -B build-22-no-acm \
  -DCMAKE_BUILD_TYPE=Debug \
  -DJTAG_ACM_TRACE=OFF \
  -DUART_FORWARD_PORT_SELECT=DISABLED \
  -DCOPY_ELF_TO_SOURCE=OFF
cmake --build build-22-no-acm -j4
"/home/dxxdx/pub/RISC-V Embedded GCC15/bin/riscv32-wch-elf-objcopy" \
  -O binary build-22-no-acm/CH32_FT232.elf build-22-no-acm/CH32_FT232.bin
```

工程使用配置中指定的 WCH GCC15、`rv32imacxw` 和 `-Og`。每次构建会生成新的 USB 串号，重新构建后的产物应按新串号记录。

## 板上验证结果

以下为已有实测记录，详见 [成功记录](docs/gw1n9-flash-success-20260921.md) 和 [原始结果汇总](logs/gw1n9-dr-transaction-20260921-205822/validation.json)。本文整理没有重新操作硬件。

- 已验证 MCU 固件串号：`CH32_FTDI_260921125322`，固定 22PINOUT，ACM 调试及 UART 转发关闭。
- FPGA 原始 IDCODE：`0x1100481B`，同一块 GW1N(R)-9C 板。
- 完整测试文件：`/home/dxxdx/FPGAProjects/9K_LED_project.fs`，预期校验值 `0x63BD`。

| 检查 | 结果 |
| --- | --- |
| 单页擦除、写入 64 字图样、实际 TDO 读回 | 64 字零差异，包含 `01234567`、`89ABCDEF`、walking-one 和反相图样 |
| 擦除后重新加载 | 状态从 `0x39020` 变为 `0x19020`，Flash Lock 清零 |
| openFPGALoader v0.13.1 完整 Flash 烧录 | 约 15.35 秒，明确输出 `CRC check: Success`，最终状态 `0x1F020`、DONE=1 |
| 重新打开 USB，独立读取并只发重新加载命令 | 加载前后均为状态 `0x1F020`、USERCODE=`0x000063BD`，没有下载 SRAM 镜像 |
| FPGA 完全断电重上电 | 用户现场确认 LED 仍闪烁 |

完整 FS 的 CRC 检查是 openFPGALoader 对 USERCODE/checksum 的比较；逐字实际读回覆盖单页 64 字，未做整片逐字节读回。该结论结合了明确的 CRC Success、独立重新加载和用户冷启动观察，不能只凭进程退出码 0 判断。

当前最终固件尚未重测 Gowin Programmer GUI、Windows、外置 Flash 和其他型号；也没有在同一块 9C 上做 BL702 硬件对照。

## Diff 核对发现与交付边界

1. **新增必需源码尚未跟踪。** 普通 `git diff` 不显示 `JtagTraceConfig.h`、`boardtype/22pinout.h`、`boardtype/BoardConfig.h`、`diagnostics/jtagTrace.c/.h` 等新增文件；后续整理提交时需要一并纳入，否则仅应用已跟踪文件的补丁无法构建完整工程。
2. **当前根目录 ELF 与已验证构建身份不同。** 检查时 `CH32_FT232.elf` 串号为 `CH32_FTDI_260921131804`，大小 504164 B，SHA256 为 `9c38bf0e893849a0992a83cac64422fd325f41966fd8410583ed703b09a4aeff`。现有 `cmake-build-debug-rv32` 缓存为追踪 OFF、UART PB67；这份根目录 ELF 尚无对应的板测记录。
3. **已验证固件归档路径目前不存在。** 历史 BIN 为 `firmware-22pinout-dr-transaction-20260921-2053/CH32_FT232_22PINOUT_DR_TRANSACTION.bin`，记录的 SHA256 为 `201bafbdef2b5c5326f15daea21bec1c2c56a91897c63ddc0c1006743bf5e017`。写本文时该目录已不在工作区，成功日志仍保留。
4. **无超时批次策略有确定边界。** 编程收集停在小于 4096 B 的 64 B 整数倍时，ZLP 不能使其完成；恰好 4096 B 也需要下一次 OUT。主机停止发送时会一直等待，这是当前保留的 BL702 行为。
5. **Gowin 特殊处理影响通用 MPSSE 行为。** 长时钟识别只检查非读字节命令长度不小于 8000、首字节为零，未要求当前 IR 为擦除，也未验证剩余字节全零；满足条件的普通数据可能被替换为长时钟。参考代码的 MSB `suppress_gpio` 批次作用域也被保留。当前实现的验证范围是上述烧录路径。

新增的 [ACM 捕获工具](host_tools/capture_acm_trace.py)、[USB Flash 诊断工具](host_tools/README.md) 和 [独立直驱诊断](diagnostics/gw1n9_direct/README.md) 用于保存命令、阶段状态及实际读回证据。直驱诊断使用独立固件入口，已从正常固件构建中排除；带擦写参数的诊断会改变 FPGA 内容。旧诊断记录保留各自的失败结果和时序参数，最新成功结论以本节链接的实测记录为准。

本次文档整理仅拉取远程引用、静态核对差异和已有日志，并检查差异空白格式；未修改固件逻辑、重新编译、运行测试套件、刷写设备或提交推送。
