# 更新说明：GW1N(R)-9C Flash 修复与板级配置重构

Flash 修复及实测日期：2026-09-21。发布文档整理：2026-09-22。

本次更新修复 CH32 下载器可以下载 SRAM、但 GW1N(R)-9C 内置 Flash 擦写后验证失败的问题，并将硬件选择统一为 `userconfig.cmake` → `boardtype`。新增 PinSelectGUI，支持保存预设或自定义引脚后直接在 CLion 构建，最终配置显示在构建日志末尾。

## Flash 烧录修复

对比基线为历史提交 `668b64e4fc595e6d8f39ce6e5986dad297bccd70`；这里不表示远程 master 目前仍停留在该提交。BL702 源码参考提交为 `1b27d3f74dfc7cfb157e6560ca8a3e7d964b391a`，对照的是实际编译的 `firmware/app/usb2uartjtag/jtag_process.c`。

| 项目 | 历史基线 | 本次实现 |
| --- | --- | --- |
| JTAG RX / TX | 4096 / 512 B | 4096 / 1024 B |
| 收包 | 合并接收，保留 2 ms 间隔超时 | 普通 OUT 立即执行，匹配编程页头后按 BL702 收集；无超时 |
| 批次执行 | 有时钟预算，可因 TX 背压让出 | 整批屏蔽可屏蔽中断执行，不拆批回包 |
| Flash DR32 | 已有整字合并和延迟退出 | 暂存匹配的进入、数据和退出命令，完整事务连续执行 |
| 擦除等待 | 按 HCLK 时基输出约 180 ms | MCU 本地连续输出 600000 拍 |
| USB 交接 | 旧状态包和复位门控 | 对齐 BL702 回包计时，明确复位与新 OUT 的交接顺序 |

基线 RX 已是 4 KiB；容量变化是 TX 从 512 B 增至 1 KiB，不能把这次修复归结为 RX 扩容。

对于匹配的 `0x71` 编程和 `0x75` 首个擦除地址，先收齐三拍进入 Shift-DR、24+7+1 位数据及返回 Idle 的后缀，再执行：

```text
Idle → 三拍进入 Shift-DR → 连续 DR32 → 两拍 Update/Idle → 本地等待
```

物理事务开始后不再穿插 USB 取包或下一条命令解析。准备、擦除、首地址和数据字后的等待分别为 1200、600000、32、24 拍；实际耗时依赖 MCU 与编译结果；主机设置的 TCK 分频在本实现中不生效。

前一版在相同缓冲、等待拍数和 GPIO 循环下仍失败；进一步暂存进入序列后通过。证据支持完整 DR 事务连续执行是起效的关键，但没有测得 FPGA 可容忍的最大间隙，也不能据此断言发生过 USB 丢包。实现及保留限制见 [JTAG 内核说明](jtag_core/jtagManager.md)。

## 配置与构建更新

- 硬件差异统一进入 `boardtype`：移除 `GPIO_Cfg.h`、`UartForwardConfig.h`、`LED/LedInstancesConfig.h`；GPIO 执行实现迁至 `boardtype/BoardGpio.c`。
- `BoardConfig.h` 校验唯一板型和必需的引脚、UART/DMA/IRQ、LED 等定义，缺少配置直接报错。
- PinSelectGUI 提供 20PINOUT、22PINOUT 和自定义模式。无配置文件时默认显示 20PINOUT，已有文件时恢复保存值；自定义支持四个不同的 PA0～PA7 JTAG 引脚，以及 PA23、PB67 或禁用 UART。
- GUI 只保存根目录 `userconfig.cmake`。CMake 解析并校验配置数据，预设使用板型头，自定义生成构建目录中的 `generated/userBoardConfig.h`。
- 配置文件变动触发 CMake 自动重新配置。CLion 直接构建 `CH32_FT232` 目标，无需另传硬件宏。
- 构建末尾在产物生成、可选 ELF 复制和 WCH 分析之后，打印本次固件实际采用的引脚和功能开关。
- CMake 每次构建独立生成 UTC 秒级 USB 串号。`RUN_LED_ENABLE`、`JTAG_ACM_TRACE` 在新构建目录默认关闭；启用 ACM 调试时关闭 UART 转发。

## 从旧配置迁移

先按本机安装位置修改 `CMakeLists.txt` 的 `TOOLCHAIN_PATH`，再运行：

```sh
python3 PinSelectGUI/pinselect.py
```

保存接线对应的配置，然后直接在 CLion 构建，或执行：

```sh
cmake -S . -B build-user -DCOPY_ELF_TO_SOURCE=OFF
cmake --build build-user --target CH32_FT232 -j4
```

旧 `BOARD_TYPE`、`UART_FORWARD_ENABLE`、`UART_FORWARD_PORT_SELECT` CMake 选项不再覆盖硬件配置。20/22PINOUT 预设均启用 PB6/PB7 UART；如果需要复现已验证固件的 **22PINOUT 接线且 UART 关闭**，在 GUI 先选 22PINOUT，再切换自定义，将 UART 设为禁用，并保持 `JTAG_ACM_TRACE=OFF`。

已有 CMake cache 会保留运行灯和调试开关。切换到 22PINOUT 或自定义时，若旧缓存开了运行灯，需要设置 `RUN_LED_ENABLE=OFF`。完整格式及约束见[板级配置说明](boardtype/README.md)。

## 验证记录与范围

Flash 实测对应固件串号 **`CH32_FTDI_260921125322`**，22PINOUT 接线，UART 和 ACM 调试关闭；FPGA 原始 IDCODE 为 `0x1100481B`。

| 检查 | 结果 |
| --- | --- |
| 单页擦除、写入和实际 TDO 读回 | 64 字零差异，覆盖固定值、walking-one 及反相图样 |
| 擦除后重新加载 | 状态 `0x39020` → `0x19020`，Flash Lock 清零 |
| openFPGALoader v0.13.1 完整 LED 文件烧入内置 Flash | 明确输出 `CRC check: Success`，最终状态 `0x1F020`、DONE=1 |
| 重新打开 USB，独立从 Flash 重新加载 | 前后均为状态 `0x1F020`、USERCODE=`0x000063BD`，未下载 SRAM 镜像 |
| FPGA 完全断电重上电 | 用户现场确认 LED 仍闪烁 |

完整文件校验是 openFPGALoader 对 USERCODE/checksum 的比较；实际逐字读回覆盖单页 64 字，没有做整片逐字节读回。退出码 0 本身不作为成功依据。固件和输入校验值见[成功记录](docs/gw1n9-flash-success-20260921.md)。

后续配置重构已做预处理、语法和引脚映射核对；GUI 与 CMake 做过配置读写、冲突拒绝、自动重新配置检查。已有用户提供的 CLion 构建成功日志；末尾配置摘要在生成的构建规则中核对了顺序。这些检查不等于板上 Flash 回归，后续配置变体尚无新的完整板测记录。

最终 Flash 修复固件尚未重测 Gowin Programmer GUI、Windows、外置 Flash 或其他型号，也没有同板 BL702 硬件对照。

## 源码与固件发布内容

源码需要包含 `PinSelectGUI`、新增 `boardtype` 文件、`cmake/loadUserConfig.cmake` 和所选 `userconfig.cmake`。`diagnostics/jtagTrace.c`、`diagnostics/jtagTrace.h` 也是正常工程依赖；即使 ACM 调试关闭，仍需其头文件。忽略诊断目录时，应确保这两个文件单独纳入版本控制。

`logs/`、`host_tools/` 和独立直驱诊断属于本地排查材料，历史文档中的路径不表示源码发布包自带这些文件。历史成功固件的归档路径仅用于追溯，不能当作当前可下载产物。

重新构建会改变串号。发布二进制时应附带本次配置、固件串号及校验值，并按实际板测结果注明范围；根目录 ELF 不能仅凭文件名视为上述已验证固件。
