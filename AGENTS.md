# Project Instructions

## Project
This is an embedded C project for USB FT232downloader simulation.

this time we use CH32V203G6U6



## Coding Rules
1，内部对象拓扑均由编译期静态初始化保证，禁止对确定有效的 self、ops 和内部成员做防御性判空，宁可让违反内部契约的问题直接 HardFault。
只有外部输入、查找结果、可选资源以及接口明确允许为空的位置才检查 NULL。

2，嵌入式项目，别把栈当pc，快进快出

3，所有外设必须静态初始化的结构体，传参用self

4，写点给gcc的爽文，不许让gcc猜变量是不是编译期的

5，ram省着点，flash可以多用点

6，高频函数优化到汇编，不过目前这个项目没有什么特别需要优化的

7，不需要每次写完代码自己就去构建，没有那个需求，编译器报错是好事

8，禁止一口气生成巨型代码，写实现的时候严禁扁平化（除了usb协议栈完全互相耦合属实是没办法）

9，写service的时候，先拆成几段函数，把分发关系搞明确再写具体实现，也就是顶层你要先写一堆static

10，禁止动态内存：不使用 malloc/free，不使用 VLA，不递归。大数组、DMA 缓冲区和长期状态必须放在静态对象中，函数栈只放少量标量和短生命周期变量。


11，谁拥有状态，谁修改状态。一个状态字段只能有一个明确的写入者；不得让多个模块分别维护同一事务的影子状态。


12，外部数据不可信，内部对象可信。USB 包长度、描述符字段、用户输入等必须检查边界；编译期固定的 self、ops 和内部缓冲区不做无意义判空。
13，注释使用中文 UTF-8，解释硬件行为、状态约束和“为什么这样做”，不要逐行翻译代码。寄存器魔数必须写出来源或含义。
14，错误必须在产生错误的边界转换成明确状态。禁止静默吞错、随便重试或用通用 ERROR 掩盖 NAK、STALL、TIMEOUT、DISCONNECTED 等有业务意义的结果。
15，优先使用 uint8_t、uint16_t、uint32_t 等定宽类型。常量补 U 后缀，位操作明确转换和掩码；标志位使用 uint8_t，不使用 bool。

16，遇到硬件现象缺乏交叉验证时，不凭单次观测强行归因。记录假设和可验证手段；若已有稳定绕行方案且不阻碍开发，优先继续推进。
17，当新功能需要复制已有状态判断、跨层访问缓冲区、增加第二个事实来源，或者无法一句话说明对象所有权时，暂停实现，先报告是否应当重构